//add everything here

plugins{
    id 'maven-publish'
}
group 'Stage2.module1.grandle'
version '1.0.0'

repositories {
    mavenCentral()
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')